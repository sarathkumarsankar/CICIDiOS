# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RefinementCalculator' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'AppCenter/Analytics'
  pod 'AppCenter/Crashes'
  # Pods for RefinementCalculator

  target 'RefinementCalculatorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RefinementCalculatorUITests' do
    # Pods for testing
  end

end
